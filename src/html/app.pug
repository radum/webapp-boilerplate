<!doctype html>
<html class="no-js" lang="">
	<head>
		include includes/head-content
	</head>
	<body>
		<!-- Express app renders `/pages/{page}.pug` files, each page gets injected here via nested attributes -->
		block p-page

		<!-- Body bottom content (js runtime, js scripts, service worker, analytics, etc.)
		include includes/body-bottom-content

		if (app.isProd === false)
			include includes/browser-sync.html
		<!-- Built with love using RaduM's ES6 Web app boilerplate -->
	</body>
</html>
